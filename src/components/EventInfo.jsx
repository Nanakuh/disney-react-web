/*function EventInfo () {
  return(
  <div className="event card">
      <p>Entradas seleccionadas: {tickets}</p>
      <p>Precio total: {totalPrice} €</p>
      <button onClick={handleIncrement}>Añadir Entrada</button>
      <button onClick={handleDecrement}>Eliminar Entrada</button>
      <button
        onClick={() =>
          alert(
            `¡Compra exitosa de ${tickets} entradas para ${selectedGroup} por un total de ${totalPrice} €!`
          )
        }
      >
        Comprar
      </button>
  </div>
  );
}
export default EventInfo;*/